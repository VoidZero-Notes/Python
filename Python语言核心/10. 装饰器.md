装饰器（decorator）是一种**语法糖**：用 `@` 把一个可调用对象包在另一个外面，常用来在不改原函数定义的前提下增强行为（日志、计时、鉴权等）。

| Python | TypeScript 对应 |
| ------ | ------- |
| `@decorator` 语法糖 | `@decorator`（需开启装饰器相关编译选项） |
| 装饰器（高阶函数） | 装饰器工厂 / 返回新函数的包装器 |
| 装饰自由函数 | **无直接对应**（TS 装饰器主要挂在 class / method 等上） |
| `@a` `@b` 叠加 | 多个 `@` 叠加（靠近声明的先包） |
| `functools.wraps` | **无直接对应**（需手动保留 `name` 等） |

## 装饰器的本质

装饰器本质上是一个接受函数作为参数并返回新函数的高阶函数：

```python
def my_decorator(func):
  def wrapper():
    print("函数执行前")
    func()
    print("函数执行后")
  return wrapper

# 下面的代码
def say_hello():
  print("Hello!")
say_hello = my_decorator(say_hello)

# 等效于
@my_decorator
def say_hello():
  print("Hello!")

say_hello()
# 输出：
# 函数执行前
# Hello!
# 函数执行后
```

TypeScript 类比（`@` 用在类方法上；语义仍是「包装后再替换」）：

```ts
function myDecorator(
  _target: object,
  _key: string | symbol,
  descriptor: PropertyDescriptor,
) {
  const original = descriptor.value as () => void
  descriptor.value = function wrapper(this: unknown) {
    console.log("函数执行前")
    original.call(this)
    console.log("函数执行后")
  }
  return descriptor
}

class Greeter {
  @myDecorator
  sayHello() {
    console.log("Hello!")
  }
}

new Greeter().sayHello()
// 输出：
// 函数执行前
// Hello!
// 函数执行后
```

> **说明：** Python 的 `@` 可装饰自由函数；TypeScript 装饰器主要作用在 **类及相关成员** 上。若只谈「高阶函数包装」本身，两边写法相同：`fn = decorator(fn)`，不必依赖 `@`。

**装饰器是一个可调用对象，接收一个可调用对象，返回任意对象。但为了保证程序能正常运行，通常返回另一个可调用对象来替代原对象。**

## 多个装饰器叠加

可以同时使用多个装饰器，执行顺序为从下到上：

```python
@decorator_a
@decorator_b
def func():
  pass

# 等效于：
# func = decorator_a(decorator_b(func))
```

TypeScript 类比：

```ts
function decoratorA(
  _t: object,
  _k: string | symbol,
  descriptor: PropertyDescriptor,
) {
  console.log("A 包装")
  return descriptor
}

function decoratorB(
  _t: object,
  _k: string | symbol,
  descriptor: PropertyDescriptor,
) {
  console.log("B 包装")
  return descriptor
}

class Demo {
  @decoratorA
  @decoratorB
  method() {}
}

// 靠近声明的先应用：先 B 后 A
// 约等于 method = decoratorA(decoratorB(method))
```
